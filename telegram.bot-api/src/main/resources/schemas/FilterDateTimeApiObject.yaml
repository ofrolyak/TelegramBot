title: DateTimeApiRequest
type: object
properties:
  negate:
    type: boolean
    example: false
    default: false
  onDate:
    type: string
    format: date-time
  startDate:
    type: string
    format: date-time
  endDate:
    type: string
    format: date-time
  action:
    type: string
    enum:
      - BETWEEN
      - BEFORE
      - AFTER
      - IS_NULL
