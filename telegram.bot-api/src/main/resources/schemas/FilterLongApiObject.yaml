title: LongFilterApiRequest
type: object
properties:
  onValue:
    type: integer
    format: int64
  minValue:
    type: integer
    format: int64
  maxValue:
    type: integer
    format: int64
  action:
    type: string
    enum:
      - EQUAL
      - GREATER_EQUAL
      - LESS_EQUAL
      - BETWEEN
      - IS_NULL
